{
  "name": "cvision-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "dev:all": "npx -y concurrently -k -c \"blue,green,yellow\" \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\" \"cd backend && uvicorn resume_extractor.main:app --host 127.0.0.1 --port 8000 --reload\"",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "dependencies": {
    "@clerk/nextjs": "^6.36.8",
    "mongoose": "^9.1.4"
  }
}